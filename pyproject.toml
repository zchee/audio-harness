[project]
name = "audio-harness"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Koichi Shiraishi", email = "zchee.io@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
audio-harness = "audio_harness:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
