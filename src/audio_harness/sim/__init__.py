"""Simulated-interview end-to-end evaluation (plan P4, lane E3).

Contamination-resistant by construction — persona answers are generated
fresh every run — but only a benchmark once its ranking correlates with the
real-corpus composite (Spearman rho ≥ 0.8); until then it is a smoke test.
"""
