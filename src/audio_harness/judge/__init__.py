"""Judge-based experimental evaluation lanes (plan P4).

Every lane in this package carries a pre-registered validity gate; until a
lane's gate passes for a language, its numbers render as "experimental — not
ranked" and must never feed a vendor recommendation.
"""
